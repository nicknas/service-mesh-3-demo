:icons: font
:source-highlighter: highlightjs
:highlightjs-theme: monokai
:sectnums:
:toc:
:toclevels: 4

== Despliegue de Service Mesh 3 con Gateway API y OpenTelemetry

Este directorio contiene los manifiestos de Argo CD y los charts Helm necesarios para desplegar un entorno de demostración completo en OpenShift:

* **OpenShift Service Mesh 3** (modo sidecar) con https://kubernetes.io/docs/concepts/services-networking/gateway/[Kubernetes Gateway API]
* Aplicación de ejemplo **Bookinfo**
* **Ingress gateway** basado en Gateway API
* Stack de **observabilidad**: Kiali, OpenTelemetry Collector (Helm Deployment), MinIO, TempoStack, Loki + Alloy (Helm), Grafana (Helm Deployment). Fan-out opcional a Dynatrace SaaS vía OTLP HTTP (sin Dynatrace Operator).

El despliegue se realiza mediante **OpenShift GitOps** (Argo CD). Los manifiestos `06`–`13` definen las Applications que sincronizan los charts Helm desde Git.

[IMPORTANT]
====
**Esta rama no instala operadores OLM.** El cluster destino no permite nuevas instalaciones de operadores. Se reutilizan los operadores *ya presentes* en el cluster (Service Mesh 3, Kiali, Tempo, OpenShift Logging) y, para lo que falte, se despliegan componentes vía charts Helm (OTel Collector, Grafana, Loki + Alloy).
====

[NOTE]
====
Kubernetes Gateway API viene incluida (y soportada) por defecto desde OpenShift 4.19. En versiones anteriores puede no estar instalada y no está soportada oficialmente porque es la versión de comunidad.
====

=== Requisitos previos

* Cluster OpenShift 4.16+ con operadores **preinstalados**: Service Mesh 3 (`servicemeshoperator3`), Kiali (`kiali-ossm`), Tempo (`tempo-product`) y, opcionalmente, OpenShift Logging
* Acceso de administrador de cluster (`cluster-admin`) o permisos equivalentes sobre los namespaces de la demo
* Cliente `oc` autenticado contra el cluster
* Cliente `helm` (v3)
* OpenShift GitOps (Argo CD) ya disponible
* Repositorio Git accesible desde el cluster (por defecto: `https://github.com/nicknas/service-mesh-3-demo.git`, rama `feature/dynatrace-coexistence`)

[IMPORTANT]
====
Si vas a usar tu propio fork, actualiza el campo `spec.source.repoURL` en todos los manifiestos `0X-*.yaml` antes de aplicarlos.
====

=== Arquitectura del despliegue

<!-- mermaid:service-mesh-gw-api:start -->
[source,mermaid]
----
flowchart LR
  client["my-app.hostname.com"]

  subgraph ingress["istio-ingress"]
    gateway["k8s gateway"]
  end

  subgraph bookinfo["bookinfo"]
    httproute["http route"]
    subgraph pod["bookinfo app pod"]
      direction TB
      sidecar["envoy sidecar"]
      app["app container"]
    end
  end

  client --> gateway
  gateway --> sidecar
  httproute -.-> gateway
----
<!-- mermaid:service-mesh-gw-api:end -->

Fuente editable: `images/service-mesh-gw-api.mmd` (tras editar, ejecutar `python3 utils/sync-mermaid-to-readme.py` para GitHub)

Los componentes se organizan en Applications de Argo CD independientes. **No hay Applications `01`–`05` de operadores** (no se pueden instalar en este cluster). El orden de aplicación es:

[cols="1,2,1", options="header"]
|===
| Orden | Application de Argo CD | Manifest

| Pre-req | Operadores ya instalados en el cluster | _(SM3, Kiali, Tempo; sin OLM nuevo)_
| Bootstrap | OpenShift GitOps | _(ya presente)_
| 1 | `sm3-control-plane` | `06-control-plane.yaml`
| 2 | `bookinfo-application` | `07-bookinfo-application.yaml`
| 3 | `istio-ingressgateway` | `08-istio-ingress-gateway.yaml`
| 4 | `bookinfo-gateway` | `09-bookinfo-gateway.yaml`
| 5 | `minio` | `10-minio.yaml`
| 6 | `observability` | `11-observability.yaml`
| 7 | `logging` | `13-logging.yaml`
| 8 | `grafana` | `12-grafana.yaml`
|===

[NOTE]
====
La Application `minio` debe sincronizarse **antes** que `observability`, ya que Tempo necesita el almacenamiento S3 de MinIO.

`observability` despliega: Kiali CR (operador existente), **OTel Collector como Deployment Helm** (no hay OpenTelemetry Operator), TempoStack CR (operador Tempo existente). El UIPlugin de Distributed Tracing queda desactivado (requiere Cluster Observability Operator).

`logging` despliega **Loki + Grafana Alloy** como Deployments Helm en `logging-system` (no usa Loki Operator / LokiStack).

`grafana` despliega **Grafana como Deployment Helm** en `grafana-system` (no usa Grafana Operator). Debe sincronizarse **después** de `observability` y `logging`.
====

=== Diferencias con la API clásica de Istio

* **Ingress gateway:** ya no se crea mediante un `Deployment` dedicado; se despliega un objeto `Gateway` del API `gateway.networking.k8s.io`.
* **Enrutamiento:** los `VirtualService` se sustituyen por `HTTPRoute` (o `GRPCRoute`). También existen `TCPRoute`, `TLSRoute` y `UDPRoute`, pero de momento son experimentales.
* **Otros recursos de Istio** (`DestinationRule`, `ServiceEntry`, etc.) siguen siendo aplicables.

[NOTE]
====
Los `VirtualService` todavía están soportados en Service Mesh 3, pero la recomendación es migrar a `HTTPRoute`. Existe una https://github.com/kubernetes-sigs/ingress2gateway[herramienta] para automatizar esta conversión.
====

== Instalación paso a paso

=== Paso 1: Instalar el operador OpenShift GitOps (bootstrap)

Argo CD no puede gestionarse a sí mismo en el arranque inicial. El operador GitOps se instala manualmente con el chart Helm `helm-charts/openshift-gitops-operator`:

[source,bash]
----
cd service-mesh-deployments/v3-gw-api-otel

helm template openshift-gitops-operator helm-charts/openshift-gitops-operator \
  --show-only templates/namespace.yaml \
  --show-only templates/operator-group.yaml \
  --show-only templates/subscription.yaml \
  | oc apply -f -
----

Espera a que el operador esté listo y se cree la instancia de Argo CD:

[source,bash]
----
# CSV del operador en estado Succeeded
oc get csv -n openshift-gitops-operator

# Pods de Argo CD en ejecución
oc get pods -n openshift-gitops
oc get argocd -n openshift-gitops
----

[WARNING]
====
No uses `helm install` para el chart `openshift-gitops-operator` si el operador ya ha creado la instancia `ArgoCD`: el CR es propiedad del operador y Helm fallará por conflictos de ownership. Usa siempre `helm template | oc apply` para el bootstrap.
====

=== Paso 2: Configurar RBAC de Argo CD

Por defecto, solo los administradores de cluster pueden ver las Applications en la UI de GitOps. Aplica la configuración RBAC del chart para conceder acceso de lectura a usuarios autenticados:

[source,bash]
----
helm template openshift-gitops-operator helm-charts/openshift-gitops-operator \
  --show-only templates/argocd.yaml \
  --api-versions=argoproj.io/v1beta1 \
  | oc apply -f -
----

Comprueba que la política por defecto es `role:readonly`:

[source,bash]
----
oc get configmap argocd-rbac-cm -n openshift-gitops \
  -o jsonpath='{.data.policy\.default}{"\n"}'
----

=== Paso 3: Aplicar las Applications de Argo CD

Aplica los manifiestos en orden numérico:

[source,bash]
----
for f in 06-control-plane.yaml 07-bookinfo-application.yaml 08-istio-ingress-gateway.yaml \
         09-bookinfo-gateway.yaml 10-minio.yaml 11-observability.yaml \
         13-logging.yaml 12-grafana.yaml; do
  echo "Aplicando $f..."
  oc apply -f "$f"
done
----

Comprueba el estado de las Applications:

[source,bash]
----
oc get applications -n openshift-gitops \
  -o custom-columns='NAME:.metadata.name,SYNC:.status.sync.status,HEALTH:.status.health.status'
----

=== Paso 4: Permisos del application-controller

El controlador de Argo CD necesita permisos de cluster para crear recursos de Service Mesh, Gateway API y operadores. Crea el `ClusterRoleBinding` manualmente (no está gestionado por Git):

[source,bash]
----
oc create clusterrolebinding openshift-gitops-argocd-cluster-admin \
  --clusterrole=cluster-admin \
  --serviceaccount=openshift-gitops:openshift-gitops-argocd-application-controller
----

[IMPORTANT]
====
Este paso es necesario en cada instalación limpia. Sin él, las Applications quedarán en `OutOfSync` con errores `forbidden` al sincronizar CRDs de Istio, Sail Operator o Gateway API.
====

=== Paso 5: Esperar la sincronización

Todas las Applications deben quedar en `Synced` / `Healthy`. Las Applications de operadores pueden mostrar brevemente `Degraded` en Argo CD aunque el CSV de OLM esté en `Succeeded`; es un falso positivo habitual.

Comprueba también el control plane, MinIO y logging:

[source,bash]
----
oc get istio default -n istio-system
oc get pods -n tracing-system
oc get tempostack tempo -n tracing-system
oc get clusterlogforwarder instance -n openshift-logging
oc get lokistack -n openshift-logging
oc get pods -n openshift-logging
----

=== Paso 6: Reiniciar Bookinfo para inyectar sidecars

Los pods de Bookinfo deben crearse **después** de que el control plane y el webhook de inyección estén listos. Si los pods muestran `1/1` en lugar de `2/2`, reinicia los deployments:

[source,bash]
----
oc rollout restart deployment -n bookinfo
oc rollout status deployment -n bookinfo --timeout=120s
oc get pods -n bookinfo
----

Cada pod debe mostrar `2/2 Running` (aplicación + `istio-proxy`).

== Componentes desplegados

[[operators-olm]]
=== Operadores: reutilizar los del cluster (sin instalar OLM)

Esta rama **no despliega** Subscriptions/OperatorGroups. Comprueba que el cluster ya tiene lo necesario:

[source,bash]
----
oc get subscription -A
oc get crd istios.sailoperator.io kialis.kiali.io tempostacks.tempo.grafana.com
----

[cols="2,2,3", options="header"]
|===
| Componente | En este cluster | Cómo se despliega en la demo

| **Service Mesh 3 / Sail**
| Operador preinstalado
| CRs `Istio` / `IstioCNI` (`06-control-plane`)

| **Kiali**
| Operador preinstalado
| CR `Kiali` (subchart observability)

| **Tempo**
| Operador preinstalado
| CR `TempoStack` (subchart observability)

| **OpenTelemetry Collector**
| _Sin operador_ (CRD ausente)
| **Helm Deployment** (`otel-collector`)

| **Grafana**
| _Sin operador_ (CRD ausente)
| **Helm Deployment** (`helm-charts/grafana`)

| **Loki + recolección de logs**
| _Sin Loki Operator_ (sin LokiStack)
| **Helm**: Loki + Grafana Alloy (`helm-charts/logging`)

| **Cluster Observability / UIPlugin**
| _Sin COO_
| Desactivado (`tempo-stack.uiPlugin.enabled: false`)

| **Dynatrace**
| _Sin operador_
| Fan-out OTLP HTTP opcional en el collector (sin DynaKube)
|===

Los charts bajo `helm-charts/*-operator/` quedan en el repo solo como referencia histórica; **no hay Applications Argo que los sincronicen** en esta rama.

[[operators-version-matrix]]
==== Operadores preinstalados (referencia mesh3-es-dev)

|===
| Operador | Versión observada

| OpenShift Service Mesh 3
| `servicemeshoperator3.v3.2.2`

| Kiali
| `kiali-operator.v2.17.3`

| Tempo
| `tempo-operator.v0.13.0-1`

| OpenShift Logging
| `cluster-logging` canal `stable-6.2` _(no usado por el chart logging Helm)_

| OpenShift GitOps
| `openshift-gitops-operator.v1.17.3`
|===

==== Instancias: CRs de operadores existentes + Helm standalone

[cols="2,3,2", options="header"]
|===
| Componente | Chart / recurso | Origen

| **Istio control plane** (Sail)
| `helm-charts/sm3-control-plane` → `Istio`
| Operador SM3 (preinstalado)

| **Kiali**
| `helm-charts/observability/charts/kiali` → `Kiali`
| Operador Kiali (preinstalado)

| **TempoStack**
| `helm-charts/observability/charts/tempostack` → `TempoStack`
| Operador Tempo (preinstalado)

| **OpenTelemetry Collector**
| `helm-charts/observability/charts/otel-collector` → Deployment
| Helm (sin operador)

| **Loki + Alloy**
| `helm-charts/logging` → Deployments
| Helm (sin Loki Operator)

| **Grafana**
| `helm-charts/grafana` → Deployment
| Helm (sin Grafana Operator)
|===

==== Verificar operadores preinstalados (no instalar)

[source,bash]
----
oc get subscription -A
oc get csv -A | grep -E 'servicemesh|kiali|tempo|gitops'
oc get crd istios.sailoperator.io kialis.kiali.io tempostacks.tempo.grafana.com
----

[NOTE]
====
* Los subcharts de observabilidad (`kiali`, `otel-collector`, `tempostack`) son locales (`file://`); no se empaquetan como `.tgz` en el repositorio.
* El UIPlugin de Distributed Tracing (COO) está desactivado: este cluster no tiene Cluster Observability Operator y no se puede instalar.
* Fan-out a Dynatrace SaaS es opcional vía values del collector OTel (sin DynaKube / Dynatrace Operator).
====

[[cmo-vs-coo]]
==== CMO vs COO: ¿recursos duplicados?

OpenShift incluye por defecto el **Cluster Monitoring Operator (CMO)**, que gestiona el monitoring integrado de la plataforma. El **Cluster Observability Operator (COO)** es un operador adicional que amplía la experiencia de observabilidad en consola y puede desplegar stacks propios *solo si se crean los CR correspondientes*.

===== Qué despliega cada operador

[cols="2,2,2", options="header"]
|===
| Capacidad | CMO (incluido en OCP) | COO (Application `05-cluster-observability-operator`)

| **Métricas (Prometheus)**
| Prometheus de plataforma (`openshift-monitoring`) y, si está activo, User Workload Monitoring (`openshift-user-workload-monitoring`)
| *No* despliega Prometheus al instalar el operador. Solo crea Prometheus/Alertmanager/Thanos si defines un CR `MonitoringStack` (no usado en esta demo)

| **Trazas**
| No despliega backend de trazas
| No despliega Tempo. El `UIPlugin` `distributed-tracing` enlaza la consola con el `TempoStack` del Tempo Operator

| **Consola OpenShift**
| Dashboards y alertas del monitoring por defecto
| Plugins en `Observe` (p. ej. trazas distribuidas, Perses/dashboards si se configuran)

| **Alertmanager / Thanos**
| Gestionados por CMO en los namespaces de monitoring de OpenShift
| Solo si creas un `MonitoringStack` del COO
|===

[IMPORTANT]
====
**Instalar el COO no duplica automáticamente los recursos del CMO.** Son stacks independientes según la documentación de Red Hat: el monitoring por defecto sigue siendo responsabilidad del CMO; el COO añade capacidades adicionales (consola, `MonitoringStack` opcional, Perses, etc.).
====

===== Qué usa esta demo (sin duplicar backends de CMO)

[cols="2,3", options="header"]
|===
| Necesidad | Componente en esta demo

| Métricas del mesh (sidecars, istiod)
| **CMO** — User Workload Monitoring habilitado (`cluster-monitoring-config` → `enableUserWorkload: true`) + `ServiceMonitor`/`PodMonitor` del chart `observability`

| Backend de trazas
| **Tempo Operator** + `TempoStack` (no COO ni CMO)

| Recolección OTLP desde Istio
| **OpenTelemetry Operator** + `OpenTelemetryCollector` en `istio-system`

| Trazas en Kiali / Grafana
| Kiali y Grafana consultan el mismo `TempoStack`

| Trazas en consola (`Observe → Traces`)
| **COO** + CR `UIPlugin` (`helm-charts/observability/charts/tempostack/templates/observability-plugin.yaml`)

| Dashboards
| **Grafana** (Grafana Operator). Perses vía COO no se despliega en esta demo
|===

===== Cuándo sí podría haber solapamiento

* **COO + CR `MonitoringStack`:** se crea un Prometheus/Alertmanager/Thanos *adicional* al del CMO (por diseño, para casos multi-tenant o stacks dedicados). No ocurre en esta demo porque no se aplica ningún `MonitoringStack`.
* **Selectores de scrape coincidentes:** si un `MonitoringStack` del COO y el User Workload Monitoring del CMO apuntan a los mismos `ServiceMonitor`/`PodMonitor`, ambos Prometheus podrían ingerir las mismas métricas (doble scrape, no conflicto de operadores pero sí redundancia de datos).
* **Varias UIs sobre el mismo backend:** Kiali, consola (COO) y Grafana consultan el *mismo* Tempo; no hay varios TempoStack, pero sí varios puntos de entrada para el usuario.
* **Fan-out OTel → Tempo + Dynatrace** (opcional): duplicación *intencionada* de trazas hacia dos backends distintos; ver <<dynatrace-tempo-coexistence>>.

===== ¿Puedo omitir el COO?

Sí, si no necesitas el plugin de trazas en la consola de OpenShift (`Observe → Traces`) ni Perses en consola. El stack sigue funcionando con CMO (métricas), Tempo, OTel Collector, Kiali y Grafana. Sin COO, elimina o no sincronices `05-cluster-observability-operator.yaml` y el recurso `UIPlugin` del chart `tempostack` (o deja el CR y no instales el operador — el sync fallará en el `UIPlugin`).

=== Control plane (Application 06)

El manifest `06-control-plane.yaml` despliega:

* CR `Istio` en el namespace `istio-system` (versión `v1.30.3` por defecto)
* CR `IstioCNI` en el namespace `istio-cni`
* Objeto `IstioRevision` generado automáticamente por el operador

Configuración en `helm-charts/sm3-control-plane/values.yaml`.

=== Aplicación Bookinfo (Application 07)

El manifest `07-bookinfo-application.yaml`:

* Crea el namespace `bookinfo` con las etiquetas necesarias para el mesh:
+
[source,yaml]
----
labels:
  istio-discovery: enabled
  istio-injection: enabled
----
* Despliega la aplicación https://istio.io/latest/docs/examples/bookinfo/[bookinfo]
* Configura un `PodMonitor` para métricas de los sidecars `istio-proxy` (incluye métricas de aplicación fusionadas por Istio cuando el pod declara anotaciones `prometheus.io/*`; ver `productpage-v1` en el chart bookinfo-application)

[WARNING]
====
Si el CR `Istio` no se llama `default`, la etiqueta `istio-injection=enabled` no funcionará. En ese caso usa:
[source,yaml]
----
istio.io/rev: <nombre_cr_istio>
----
====

=== Ingress gateway (Application 08)

El manifest `08-istio-ingress-gateway.yaml`:

* Crea el namespace `istio-ingress`
* Despliega un `Gateway` usando Gateway API (`gatewayClassName: istio`)
* Crea una ruta OpenShift para pruebas con el formato:
+
----
https://bookinfo.<cluster-hostname>
----

[IMPORTANT]
====
Debes usar `istio` como `gatewayClassName` para que Istio gestione el gateway creado.
====

=== Gateway de Bookinfo (Application 09)

El manifest `09-bookinfo-gateway.yaml`:

* Crea el gateway específico de Bookinfo en `istio-ingress`
* Crea el `HTTPRoute` en el namespace `bookinfo` para enrutar el tráfico externo a `productpage`

Comprueba el acceso (la ruta usa terminación TLS en el edge):

[source,bash]
----
curl -sk https://bookinfo.<cluster-hostname>/api/v1/products | jq
curl -sk -o /dev/null -w "%{http_code}\n" https://bookinfo.<cluster-hostname>/productpage
----

=== MinIO (Application 10)

El manifest `10-minio.yaml` despliega de forma independiente el almacenamiento de objetos S3 que Tempo necesita:

* Namespace `tracing-system`
* Deployment y servicio MinIO
* PVC de persistencia
* Secret con credenciales S3
* Job de creación de buckets `tempo` y `loki` (hook de Argo CD; se recrea en cada sync)

Chart: `helm-charts/minio`

Los recursos usan *sync waves* de Argo CD para coordinar el orden de creación (namespace → PVC/deployment/secret → bucket job).

=== Observabilidad (Application 11)

El manifest `11-observability.yaml` despliega:

* Instancia de **Kiali** y consola OSSM
* **OpenTelemetry Collector** para recopilar trazas y métricas del mesh
* **TempoStack** para almacenar trazas (usa el Secret de MinIO creado por la Application anterior)
* Plugin de observabilidad distribuida en la consola de OpenShift (`UIPlugin` → requiere COO; ver <<cmo-vs-coo>>)
* `ServiceMonitor` de istiod y `PodMonitor` de sidecars en `bookinfo` (métricas vía CMO / User Workload Monitoring)

Chart: `helm-charts/observability`

<!-- mermaid:sm3-otel-tempo:start -->
[source,mermaid]
----
flowchart TB
  classDef metrics fill:#e8f5e9,stroke:#2e7d32,color:#1b5e20
  classDef external fill:#fafafa,stroke:#666,color:#333

  subgraph tracing["tracing-system (TempoStack)"]
    direction TB
    object_storage[(object storage)]
    ingester[ingester]
    querier[querier]
    compactor[compactor]
    distributor[distributor]
    query_frontend[query frontend]
    gateway[gateway]

    gateway -->|span| distributor
    gateway -.->|consulta| query_frontend
    distributor --> ingester
    query_frontend --> querier
    ingester --> object_storage
    querier --> object_storage
    compactor -.-> ingester
  end

  subgraph istio["istio-system"]
    direction TB
    istiod[istiod]
    istio_service[istio service]
    service_monitor[service monitor]
    otel_collector[otel collector]
    otel_collector_service[otel collector service]

    service_monitor --> istio_service
    istio_service --> istiod
    otel_collector_service --> otel_collector
  end

  subgraph bookinfo["bookinfo"]
    direction TB
    subgraph bookinfo_pod["bookinfo app pod"]
      direction TB
      sidecar[sidecar]
      app_container[bookinfo app container]
    end
    pod_monitor[pod monitor]
  end

  prom[["prometheus (métricas)"]]:::metrics

  consulta_tempo([consulta]):::external -.-> gateway
  consulta_istio([consulta]):::external -.-> istio_service
  consulta_app([consulta]):::external -.-> app_container

  sidecar -->|trazas push| otel_collector_service
  otel_collector -->|span| gateway

  prom -.->|configura| service_monitor
  prom -.->|configura| pod_monitor
  prom ==>|métricas pull| service_monitor
  prom ==>|métricas pull| sidecar
----
<!-- mermaid:sm3-otel-tempo:end -->

Fuente editable: `images/sm3-otel-tempo.mmd` (tras editar, ejecutar `python3 utils/sync-mermaid-to-readme.py` para GitHub)

[[dynatrace-tempo-coexistence]]
==== Coexistencia Dynatrace SaaS + Tempo

En entornos donde el cliente ya usa **Dynatrace SaaS** y planea desplegar **Tempo** en sus clusters OpenShift (por ejemplo para Service Mesh, Kiali o Grafana), ambos backends **pueden coexistir sin conflicto**. No es necesario elegir uno u otro.

===== Roles complementarios

[cols="1,2,2", options="header"]
|===
| | Dynatrace SaaS | Tempo (in-cluster)

| **Propósito**
| Observabilidad corporativa (APM, infra, alerting, Smartscape…)
| Backend de trazas del stack mesh (Kiali, Grafana, consola OpenShift)

| **Consulta**
| UI Dynatrace / equipos de plataforma
| Kiali, Grafana, Jaeger UI del gateway Tempo

| **Almacenamiento**
| Tenant cloud
| Object storage en cluster (MinIO en esta demo)
|===

[[dynatrace-tempo-architecture]]
===== Diagrama de arquitectura: fan-out Tempo + Dynatrace

El patrón clave es **un solo origen de trazas** (Istio) y **un solo colector intermedio** (OpenTelemetry Collector RH), con **exportación en paralelo** a dos backends independientes. No compiten por el mismo puerto ni sustituyen uno al otro: cada uno cumple un rol distinto en consulta y almacenamiento.

Extiende el diagrama de observabilidad base (`sm3-otel-tempo.mmd`) añadiendo el fan-out hacia Dynatrace SaaS:

<!-- mermaid:sm3-otel-tempo-dynatrace:start -->
[source,mermaid]
----
flowchart TB
  classDef metrics fill:#e8f5e9,stroke:#2e7d32,color:#1b5e20
  classDef external fill:#fafafa,stroke:#666,color:#333
  classDef saas fill:#e3f2fd,stroke:#1565c0,color:#0d47a1

  subgraph tracing["tracing-system (TempoStack)"]
    direction TB
    object_storage[(object storage)]
    ingester[ingester]
    querier[querier]
    compactor[compactor]
    distributor[distributor]
    query_frontend[query frontend]
    gateway[gateway]

    gateway -->|span| distributor
    gateway -.->|consulta| query_frontend
    distributor --> ingester
    query_frontend --> querier
    ingester --> object_storage
    querier --> object_storage
    compactor -.-> ingester
  end

  subgraph dynatrace["dynatrace saas"]
    tenant["tenant *.live.dynatrace.com<br/>/api/v2/otlp"]:::saas
  end

  subgraph istio["istio-system"]
    direction TB
    istiod[istiod]
    istio_service[istio service]
    service_monitor[service monitor]
    otel_collector[otel collector]
    otel_collector_service[otel collector service]

    service_monitor --> istio_service
    istio_service --> istiod
    otel_collector_service --> otel_collector
  end

  subgraph bookinfo["bookinfo"]
    direction TB
    subgraph bookinfo_pod["bookinfo app pod"]
      direction TB
      sidecar[sidecar]
      app_container[bookinfo app container]
    end
    pod_monitor[pod monitor]
  end

  prom[["prometheus (métricas)"]]:::metrics

  consulta_tempo([consulta]):::external -.-> gateway
  consulta_istio([consulta]):::external -.-> istio_service
  consulta_app([consulta]):::external -.-> app_container
  consulta_dt([consulta]):::external -.-> tenant

  sidecar -->|trazas push| otel_collector_service
  otel_collector -->|span| gateway
  otel_collector -->|trazas otlphttp| tenant

  prom -.->|configura| service_monitor
  prom -.->|configura| pod_monitor
  prom ==>|métricas pull| service_monitor
  prom ==>|métricas pull| sidecar
----
<!-- mermaid:sm3-otel-tempo-dynatrace:end -->

Fuente editable: `images/sm3-otel-tempo-dynatrace.mmd` (tras editar, ejecutar `python3 utils/sync-mermaid-to-readme.py` para GitHub)

Leyenda del diagrama (mismo estilo que `sm3-otel-tempo.mmd`):

* **trazas (push)** — sidecar Bookinfo → `otel collector service` (`istio-system`).
* **span** — `otel collector` → gateway Tempo (`tracing-system`).
* **trazas (otlphttp)** — `otel collector` → tenant Dynatrace SaaS (`/api/v2/otlp`); fan-out en paralelo al envío a Tempo.
* **consulta** (línea discontinua) — consultas externas a gateway Tempo, `istio service` y `bookinfo app container`; UI Dynatrace → tenant SaaS.
* **configura** — Prometheus configura `service monitor` e `pod monitor`.
* **métricas (pull)** — Prometheus scrapea sidecars y ServiceMonitor (sin cambios respecto al diagrama base).

====== Flujo por capas

[cols="1,2,3,2", options="header"]
|===
| Capa | Componente | Namespace | Protocolo / puerto

| **Aplicación**
| Bookinfo (productpage, reviews, details, ratings)
| `bookinfo`
| HTTP entre servicios (mesh)

| **Mesh / export**
| Istio sidecar (`istio-proxy`)
| `bookinfo`
| Export OTLP gRPC → `otel-collector.istio-system:4317`

| **Agregación**
| OpenTelemetry Collector (`otel`)
| `istio-system`
| Receiver OTLP :4317; pipeline `traces` con dos exporters

| **Backend mesh**
| Tempo gateway → TempoStack → MinIO
| `tracing-system` / `minio`
| OTLP gRPC :8090; almacén S3

| **Backend corporativo**
| Dynatrace SaaS tenant
| _(cloud)_
| OTLP HTTP `/api/v2/otlp`; header `Authorization: Api-Token`

| **Visualización mesh**
| Kiali, Grafana, consola OSSMC
| `istio-system`, `grafana-system`
| Query API Tempo (no Dynatrace)

| **Visualización corporativa**
| UI Dynatrace
| _(cloud)_
| Distributed traces, APM
|===

====== Por qué conviven sin conflicto

* **Un único `extensionProvider` en Istio** — el mesh solo conoce al collector RH; no hay que elegir Tempo *o* Dynatrace en la configuración de Istio.
* **Fan-out en el collector** — cada span de traza se envía a *ambos* exporters del pipeline `traces`; es duplicación intencionada, no competencia.
* **Consultas separadas** — Kiali/Grafana siguen leyendo **solo Tempo**; Dynatrace tiene su propia UI y retención en SaaS.
* **Protocolos adaptados** — Istio exporta gRPC; Tempo acepta gRPC; Dynatrace SaaS requiere **HTTP** (`otlphttp/dynatrace`), traducido por el collector.
* **Secretos independientes** — Tempo usa token del ServiceAccount del collector (Bearer hacia el gateway); Dynatrace usa `otel-dynatrace-ingest` con `Api-Token`.

====== Configuración Helm (pipeline de trazas)

En `helm-charts/observability/charts/otel-collector` el pipeline queda así cuando Dynatrace está activo:

[source,yaml]
----
service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [debug, otlp/tempo, otlphttp/dynatrace]   # fan-out
----

* `otlp/tempo` → `tempo-tempo-gateway.tracing-system.svc.cluster.local:8090`
* `otlphttp/dynatrace` → `https://{ENV_ID}.live.dynatrace.com/api/v2/otlp`

====== Variantes de despliegue

[cols="2,3,3", options="header"]
|===
| Escenario | Cluster | Fichero values / Application

| **Demo workshop (bbgmb)**
| Público; sin red BBVA
| `11-observability` + `values-cluster-bbgmb-dynatrace.yaml` o script `utils/setup-dynatrace-saas-bbgmb.sh`

| **Lab / cliente con SaaS**
| Egress a `*.live.dynatrace.com`
| `11-observability` + `values-dynatrace.example.yaml`

| **Cliente BBVA Managed** _(referencia aso1)_
| Red interna; operador preinstalado
| `DynaKube` + OneAgent/ActiveGate — *no aplicable en bbgmb*; ver <<dynatrace-bbva-managed>>

| **Ingest in-cluster Dynatrace** _(opcional)_
| Con operador Dynatrace
| `14-dynatrace.yaml` + `values.example.yaml` (`saas-ingest`); collector apunta a `telemetry-ingest:4318`
|===

Para **probar en cluster-bbgmb** usa modo **SaaS** (`values-cluster-bbgmb-dynatrace.yaml`); no requiere `05-dynatrace-operator` ni `14-dynatrace`.

Componentes GitOps _(opcionales)_:

[cols="2,2,3", options="header"]
|===
| Application | Chart | Función

| `11-observability` + `values-dynatrace.example.yaml` _(o `values-cluster-bbgmb-dynatrace.yaml` en bbgmb)_
| `helm-charts/observability`
| Fan-out OTel → Tempo + Dynatrace SaaS _(modo saas)_

| `05-dynatrace-operator.yaml` + `14-dynatrace.yaml`
| `helm-charts/dynatrace-operator` / `dynatrace`
| Solo si usas modo `ingest` (gateway in-cluster vía DynaKube)
|===

Ventajas:

* **Sin cambios en Istio** ni en el flujo Kiali → Tempo de la demo.
* **Tempo** sigue siendo el backend del mesh local.
* **Dynatrace** recibe las mismas trazas para correlación con el resto de la plataforma del cliente.
* **Sin conflicto de puertos**: cada backend tiene su propio endpoint; no se instala un segundo collector de ingest mesh salvo el opcional de Dynatrace Operator (no requerido en este patrón).

[NOTE]
====
Dynatrace SaaS acepta OTLP por **HTTP/protobuf**. El collector RH recibe gRPC de Istio y exporta a Tempo (gRPC) y a Dynatrace (HTTP). Para pruebas rápidas usa modo **`saas`** (OTLP directo al tenant con `Api-Token`); no necesitas instalar el Dynatrace Operator.
====

===== Activación modo SaaS (recomendado para pruebas)

Ver diagrama completo en <<dynatrace-tempo-architecture>>.

. Crear un token en Dynatrace con scope `openTelemetryTrace.ingest`.
. Crear el Secret en `istio-system` (no commitear el token):

+
[source,bash]
----
oc create secret generic otel-dynatrace-ingest -n istio-system \
  --from-literal=apiToken='dt0c01.XXXXXXXX'
----

. Editar `helm-charts/observability/values-dynatrace.example.yaml`: sustituir `YOUR_ENV_ID` por el ID del tenant (ej. `abc12345`, sin el dominio).
. Activar fan-out en la Application `11-observability.yaml`:

+
[source,yaml]
----
source:
  helm:
    valueFiles:
      - values-dynatrace.example.yaml
----

O con helm local:

+
[source,bash]
----
helm upgrade observability ./helm-charts/observability -n istio-system \
  -f ./helm-charts/observability/values-dynatrace.example.yaml
----

. Comprobar egress HTTPS desde el cluster hacia `https://<ENV_ID>.live.dynatrace.com` (firewall/proxy corporativo).

===== Modo ingest SaaS (opcional, con operador)

Si prefieres gateway in-cluster (`telemetry-ingest:4318`), instala `14-dynatrace.yaml` con `helm-charts/dynatrace/values.example.yaml` (`dynaKube.mode: saas-ingest`). El operador puede estar preinstalado o desplegarse con `05-dynatrace-operator.yaml`.

Fichero de referencia OTel fan-out SaaS: `helm-charts/observability/values-dynatrace.example.yaml`

[[dynatrace-bbva-managed]]
===== Modo Managed BBVA (referencia: aso1-es-dev)

En clusters BBVA el operador **Dynatrace v1.5.1** suele estar preinstalado en `dynatrace-operator`. El catálogo interno Platform Core (`dynatrace-v1.5.1`) publica el **mismo operador certificado Red Hat** (`dynatrace-operator.v1.5.1`, imágenes en `registry.connect.redhat.com/dynatrace/dynatrace-operator`). Para instalarlo en otros clusters usa `certified-operators` (`05-dynatrace-operator.yaml`).

El patrón `DynaKube` desplegado es:

* **OneAgent** `classicFullStack` en nodos worker (`dynatraceOneAgent=enabled`)
* **ActiveGate** (2 réplicas) en nodos infra con capacidades `routing`, `kubernetes-monitoring`, `dynatrace-api`
* Imágenes corporativas en `dynacmc.work.es.nextgen.igrupobbva/linux/`
* `apiUrl` Managed: `https://dynacmc.work.es.nextgen.igrupobbva/e/<tenant-uuid>/api`
* Secret `dynakube` (solo `apiToken`) y ConfigMap `ca-dynatrace` (`trustedCAs`)
* `networkZone` = nombre del cluster

[cols="2,3", options="header"]
|===
| Recurso en cluster BBVA | Chart / values de esta demo

| Operador preinstalado (`dynatrace-operator` ns)
| No sincronizar `05-dynatrace-operator.yaml`

| `DynaKube` (`aso1-es-ch-es-work`)
| `14-dynatrace.yaml` + `helm-charts/dynatrace/values-bbva.example.yaml`

| OneAgent DaemonSet + ActiveGate StatefulSet
| `dynaKube.mode: managed` en el chart `dynatrace`
|===

Activación:

. Comprobar operador: `oc get dynakube,csv -n dynatrace-operator`
. Crear Secret: `oc create secret generic dynakube -n dynatrace-operator --from-literal=apiToken='...'`
. Reutilizar o crear `ca-dynatrace` (clave `certs`) si el tenant Managed lo requiere.
. Etiquetar nodos worker: `oc label node <node> dynatraceOneAgent=enabled`
. Editar `values-bbva.example.yaml`: `name`, `apiUrl`, `networkZone`, `enabled: true`
. Sincronizar `14-dynatrace.yaml` (namespace destino: `dynatrace-operator`)

Verificación:

[source,bash]
----
oc get dynakube -n dynatrace-operator
oc get pods -n dynatrace-operator | grep -E 'oneagent|activegate'
oc get ds -n dynatrace-operator
----

En clusters sin operador preinstalado (ej. ple2-es-dev solo tiene operador vacío), puedes desplegar el operador con `05-dynatrace-operator.yaml` y luego el `DynaKube`.

===== Verificación

Script automatizado (estático + cluster + egress si hay `oc` y token):

[source,bash]
----
./utils/validate-dynatrace-otlp.sh
# Solo manifiestos Helm:
./utils/validate-dynatrace-otlp.sh --static
# Probar HTTPS al tenant (requiere DT_ENV_ID y DT_API_TOKEN o Secret en cluster):
DT_ENV_ID=abc12345 DT_API_TOKEN='dt0c01.…' ./utils/validate-dynatrace-otlp.sh --egress
----

Pasos manuales:

. Genera tráfico: `./utils/generate-traffic.sh 30`
. **Tempo / Kiali:** pestaña *Traces* en un workload de Bookinfo (sin cambios).
. **Dynatrace:** en *Distributed traces*, filtrar por `service.name` (por ejemplo `productpage.bookinfo`).

Comprobar que el collector exporta sin errores:

[source,bash]
----
oc logs -n istio-system -l app.kubernetes.io/name=opentelemetry-collector --tail=50
oc get opentelemetrycollector otel -n istio-system -o yaml | grep -A3 otlphttp
----

===== Qué evitar

* No apuntar `meshConfig.extensionProviders` a Dynatrace y al collector RH a la vez.
* No activar `otlpExporterConfiguration` del Dynatrace Operator en `istio-system` / `bookinfo` si el mesh ya exporta vía collector (riesgo de duplicidad).
* No usar exporter `otlp` (gRPC) hacia el tenant SaaS de Dynatrace; usar `otlphttp`.

===== Mensaje ejecutivo (cliente)

[quote]
Dynatrace SaaS y Tempo pueden desplegarse en el mismo cluster OpenShift sin conflicto. La arquitectura recomendada centraliza la telemetría del mesh en un OpenTelemetry Collector y configura exportación en paralelo: Tempo para el ecosistema Service Mesh (Kiali, Grafana) y Dynatrace para la observabilidad corporativa en SaaS. No es necesario renunciar a Tempo por usar Dynatrace, ni viceversa.
[/quote]

=== Logging (Application 13)

El manifest `13-logging.yaml` despliega **Loki + Grafana Alloy** como charts Helm standalone (sin Loki Operator ni LokiStack):

* Chart: `helm-charts/logging` → Deployments en namespace `logging-system`
* Alloy lee stdout de pods Bookinfo vía API Kubernetes y escribe en Loki
* Grafana consulta `http://logging-loki.logging-system.svc:3100`

==== Orden de despliegue

. `10-minio.yaml` _(opcional para logs; MinIO es para Tempo)_
. `13-logging.yaml`
. `12-grafana.yaml` (después de logging)

==== Arquitectura

[source]
----
Pods Bookinfo (stdout/stderr)
        │
        ▼
Grafana Alloy (logging-system) ── API Kubernetes pods/log
        │
        ▼
Loki (logging-system, PVC)
        │
        ▼
Grafana (datasource Loki + dashboard Bookinfo — Logs)
----

==== Componentes desplegados

[cols="1,2", options="header"]
|===
| Recurso | Descripción

| Namespace `logging-system` | Namespace dedicado (Helm)
| **Loki** | Almacén de logs (Deployment + PVC)
| **Grafana Alloy** | Recolección de logs de Bookinfo
|===

==== Qué se recoge (y qué no)

* **Sí:** stdout/stderr de contenedores de aplicación en namespace `bookinfo` (`productpage`, `reviews`, `details`, `ratings`), filtrando `istio-validation`.
* **No:** logs de plataforma / auditoría OpenShift (no usa ClusterLogForwarder).

==== Consulta en Grafana

En Grafana Explore, filtra con LogQL:

[source,logql]
----
{namespace="bookinfo", app="productpage"}
{namespace="bookinfo", app="reviews"}
----

==== Cambios en Bookinfo y mesh (visibilidad de logs)

* **productpage:** el chart sobrescribe el comando de Gunicorn con `--access-logfile -` para enviar el access log a stdout.
* **reviews:** Open Liberty no registra cada petición en stdout; se habilitan access logs de Envoy (`Telemetry` `access-logs` + `meshConfig.accessLogFile: /dev/stdout`) para que el colector de plataforma recoja el contenedor `istio-proxy`.
* **LOG_DIR:** no se usa en reviews (los logs de Liberty iban a ficheros bajo `/tmp/logs`, invisibles para el colector de Kubernetes).

==== Configuración del ClusterLogForwarder

El fichero `helm-charts/logging/templates/cluster-log-forwarder.yaml` define:

. Input `application` (logs de aplicación del cluster).
. Output `lokiStack` apuntando al `LokiStack` `logging` en `openshift-logging`.
. Autenticación con token del ServiceAccount `collector` y CA del gateway en `logging-gateway-ca-bundle`.

Parámetros en `helm-charts/logging/values.yaml` → bloque `clusterLogForwarder`.

==== Verificación

[source,bash]
----
oc get csv -n openshift-operators-redhat | grep cluster-logging
oc get clusterlogforwarder instance -n openshift-logging
oc get lokistack logging -n openshift-logging
oc get pods -n openshift-logging
# Tras generar tráfico a Bookinfo, en Grafana Explore (datasource Loki):
#   {kubernetes_namespace_name="bookinfo"}
----

=== Grafana (Application 12)

El manifest `12-grafana.yaml` despliega **Grafana como Deployment Helm** (sin Grafana Operator):

* Namespace `grafana-system`
* Deployment + Service + Route + PVC
* Datasources provisionados (ConfigMap): **Prometheus** (Thanos Querier), **Tempo** (tenant `bookinfo`) y **Loki** (standalone en `logging-system`)
* Dashboards provisionados en carpeta *Bookinfo*:
** *Bookinfo — Escenario OK*
** *Bookinfo — Escenario Fallo*
** *Bookinfo — Logs*

Chart: `helm-charts/grafana`

Credenciales por defecto (demo): usuario `admin`, contraseña `grafana` (`helm-charts/grafana/values.yaml`).

Acceso:

[source,bash]
----
oc get route -n grafana-system -o jsonpath='https://{.items[0].spec.host}{"\n"}'
oc get deploy,pods -n grafana-system
----

==== Integración de Kiali con Tempo (pestaña Traces)
[[kiali-traces]]

Por defecto, el CR de Kiali solo configura Prometheus para métricas. Sin `external_services.tracing`, Kiali no sabe dónde consultar trazas y la pestaña *Traces* no aparece en workloads ni aplicaciones, aunque el flujo de tracing (Istio → OTel Collector → Tempo) esté funcionando.

La configuración se define en `helm-charts/observability/charts/kiali/templates/kiali.yaml` y sus valores en `helm-charts/observability/charts/kiali/values.yaml`.

===== Bloque `external_services.tracing`

[source,yaml]
----
tracing:
  enabled: true
  provider: tempo
  use_grpc: false
  internal_url: https://tempo-tempo-gateway.tracing-system.svc.cluster.local:8080/api/traces/v1/bookinfo/tempo
  external_url: https://tempo-tempo-gateway-tracing-system.apps.<cluster-domain>/api/traces/v1/bookinfo/search
  health_check_url: https://tempo-tempo-gateway.tracing-system.svc.cluster.local:8080/api/traces/v1/bookinfo/tempo/api/echo
  auth:
    ca_file: /var/run/secrets/kubernetes.io/serviceaccount/service-ca.crt
    type: bearer
    use_kiali_token: true
  tempo_config:
    name: tempo
    namespace: tracing-system
    tenant: bookinfo
    url_format: jaeger
----

[cols="1,3", options="header"]
|===
| Campo | Descripción

| `enabled` | Activa la integración con el backend de tracing.
| `provider: tempo` | Indica que el backend es Tempo (Red Hat OpenShift distributed tracing platform).
| `internal_url` | URL interna que Kiali usa para *consultar* trazas. Es imprescindible para mostrar la pestaña *Traces* integrada en la UI.
| `external_url` | URL del enlace *View in Tracing* hacia la consola Jaeger expuesta por el gateway de Tempo.
| `health_check_url` | Endpoint `/api/echo` que Kiali usa para comprobar que Tempo responde.
| `auth` | Autenticación HTTPS con el token del ServiceAccount de Kiali. Necesario en OpenShift con Tempo en modo multitenancy (`openshift`).
| `tempo_config.tenant` | Debe coincidir con el `tenantName` definido en el TempoStack (`bookinfo` en este ejemplo).
| `tempo_config.url_format: jaeger` | Formato de los enlaces externos hacia la UI Jaeger del gateway.
|===

[IMPORTANT]
====
`internal_url` habilita la pestaña *Traces* dentro de Kiali.
`external_url` solo sirve para el enlace externo *View in Tracing*. Sin `internal_url`, Kiali redirige fuera pero no muestra trazas en su propia UI.
====

===== URLs generadas automáticamente

El fichero `helm-charts/observability/charts/kiali/templates/_helpers.tpl` construye las URLs a partir del TempoStack desplegado:

* **Gateway interno:** `tempo-tempo-gateway.tracing-system.svc.cluster.local:8080`
* **Tenant:** `bookinfo` (definido en `tempostack.yaml`)
* **Ruta externa:** `tempo-tempo-gateway-tracing-system.apps.<cluster-domain>`

Si despliegas en otro cluster, actualiza `tracing.clusterDomain` en `helm-charts/observability/charts/kiali/values.yaml`:

[source,yaml]
----
tracing:
  clusterDomain: apps.<tu-cluster>.<dominio>
----

===== Permisos RBAC

En modo multitenancy de Tempo (`spec.tenants.mode: openshift`), el gateway valida permisos por tenant. El ServiceAccount `kiali-service-account` se añade al `ClusterRoleBinding` `tempostack-traces-reader` (`helm-charts/observability/charts/tempostack/templates/cluster-role-binding.yaml`) para que Kiali pueda leer trazas del tenant `bookinfo`.

===== Flujo de trazas

. Istio genera spans (Telemetry CR con provider `otel`, sampling al 100 %).
. El OpenTelemetry Collector los reenvía a Tempo vía OTLP (y opcionalmente a Dynatrace SaaS; ver <<dynatrace-tempo-coexistence>>).
. Tempo los almacena en MinIO.
. Kiali consulta Tempo mediante `internal_url` y muestra la pestaña *Traces* en workloads y aplicaciones.

==== Verificación de la pestaña Traces

. Genera tráfico con `./utils/generate-traffic.sh 30`.
. Abre Kiali → *Workloads* → selecciona un workload de `bookinfo` (por ejemplo `productpage`).
. Comprueba que aparece la pestaña *Traces* con el scatterplot de spans.

image::images/trazas-kiali.png[Pestaña Traces en Kiali mostrando el scatterplot y el detalle de una traza de productpage]

Si no aparece, verifica:

[source,bash]
----
# Kiali tiene tracing habilitado
oc get kiali kiali -n istio-system \
  -o jsonpath='{.spec.external_services.tracing.enabled}{" "}{.spec.external_services.tracing.internal_url}{"\n"}'

# TempoStack y gateway en ejecución
oc get tempostack tempo -n tracing-system
oc get pods -n tracing-system -l app.kubernetes.io/component=gateway
----

== Generación de tráfico y verificación

=== Script de tráfico externo

El script `utils/generate-traffic.sh` genera tráfico HTTPS contra los endpoints `/productpage` y `/api/v1/products`.

Configura el hostname de tu cluster en el script:

[source,bash]
----
HOSTNAME=bookinfo.apps.<tu-cluster>.<dominio>
----

Ejecución:

[source,bash]
----
./utils/generate-traffic.sh 30
----

Una ejecución correcta muestra códigos `200/200` en cada iteración.

=== Tráfico interno al cluster

En `utils/internal-traffic/` hay un Dockerfile y manifiestos para desplegar un generador de tráfico dentro del cluster. Consulta `utils/README.adoc` para más detalles.

=== Kiali

Tras generar tráfico, abre Kiali desde la consola de OpenShift (sección Service Mesh) o la ruta del operador. Deberías ver:

* El grafo de tráfico de Bookinfo con el ingress `istio-ingress-istio` (Gateway API).
* La pestaña *Traces* en el detalle de cada workload, con spans recopilados desde Tempo (ver <<kiali-traces>>).

image::images/trafico-kiali.png[Grafo de tráfico de Bookinfo en Kiali]

image::images/trazas-kiali.png[Pestaña Traces en Kiali con detalle de spans de productpage]

[NOTE]
====
No hagas scrape directo de los puertos de aplicación de Bookinfo (`:9080/metrics`) mediante un `ServiceMonitor` o un segundo endpoint en el `PodMonitor`: genera tráfico fantasma `unknown → service` en Kiali.

Para métricas de aplicación usa *metrics merging* de Istio: declara `prometheus.io/scrape`, `prometheus.io/port` y `prometheus.io/path` en el pod (como en `productpage-v1`); el `istio-agent` scrapea la app dentro del pod y expone todo en `/stats/prometheus` del sidecar. Prometheus solo necesita el `PodMonitor` del chart de observabilidad, que scrapea `istio-proxy`.
====

== Accesos útiles

[cols="1,2"]
|===
| Recurso | URL / comando

| Bookinfo (productpage) | `https://bookinfo.<cluster-hostname>/productpage`
| Argo CD (GitOps) | Consola OpenShift → Operadores → OpenShift GitOps, o ruta `openshift-gitops-server-openshift-gitops.apps.<cluster-hostname>`
| Kiali | Consola OpenShift → Service Mesh → Kiali
| Grafana | `oc get route grafana -n grafana-system`
| Logs (Loki gateway) | `https://logging-gateway-http.openshift-logging.svc:8080/api/logs/v1/application` (interno, requiere token)
| Trazas (Tempo / Jaeger UI) | Ruta del gateway de Tempo en `tracing-system`
|===

== Charts Helm de referencia

[cols="1,2"]
|===
| Application | Chart

| `service-mesh3-operator` | `helm-charts/sm3-operator`
| `kiali-operator` | `helm-charts/kiali-operator`
| `otel-operator` | `helm-charts/otel-operator`
| `tempo-operator` | `helm-charts/tempo-operator`
| `loki-operator` | `helm-charts/loki-operator`
| `openshift-logging-operator` | `helm-charts/cluster-logging-operator`
| `cluster-observability-operator` | `helm-charts/cluster-observability-operator`
| `grafana-operator` | `helm-charts/grafana-operator`
| `dynatrace-operator` _(opcional)_ | `helm-charts/dynatrace-operator`
| `dynatrace` _(opcional)_ | `helm-charts/dynatrace`
| `sm3-control-plane` | `helm-charts/sm3-control-plane`
| `bookinfo-application` | `helm-charts/bookinfo-application`
| `istio-ingressgateway` | `helm-charts/istio-ingressgateway`
| `bookinfo-gateway` | `helm-charts/bookinfo-gateway`
| `minio` | `helm-charts/minio`
| `observability` | `helm-charts/observability`
| `logging` | `helm-charts/logging`
| `grafana` | `helm-charts/grafana`
| Bootstrap GitOps (manual) | `helm-charts/openshift-gitops-operator`
|===

== Desinstalación completa

Para eliminar todo el entorno de demostración y dejar el cluster limpio:

[source,bash]
----
# 1. Eliminar Applications de Argo CD
oc delete applications --all -n openshift-gitops --wait=true

# 2. Eliminar CRs de mesh y observabilidad
oc delete istio --all -n istio-system --ignore-not-found
oc delete istiocni --all -n istio-cni --ignore-not-found
oc delete kiali --all -n istio-system --ignore-not-found
oc delete opentelemetrycollector --all -n istio-system --ignore-not-found
oc delete tempostack --all -n tracing-system --ignore-not-found
oc delete lokistack --all -n openshift-logging --ignore-not-found
oc delete dynakube --all -n dynatrace-operator --ignore-not-found
oc delete grafana,grafanadatasource,grafanadashboard --all -n grafana-system --ignore-not-found

# 3. Revertir RBAC de GitOps al valor por defecto
oc patch argocd openshift-gitops -n openshift-gitops --type merge \
  -p '{"spec":{"rbac":{"defaultPolicy":"","policy":"g, system:cluster-admins, role:admin\ng, cluster-admins, role:admin\n","scopes":"[groups]"}}}'

# 4. Eliminar ClusterRoleBinding manual
oc delete clusterrolebinding openshift-gitops-argocd-cluster-admin --ignore-not-found

# 5. Desinstalar operadores (subscriptions y CSVs)
oc delete subscription openshift-gitops-operator -n openshift-gitops-operator --ignore-not-found
oc delete subscription servicemesh3-operator kiali-operator opentelemetry-operator grafana-operator \
  -n openshift-operators --ignore-not-found
oc delete subscription tempo-operator -n openshift-tempo-operator --ignore-not-found
oc delete subscription loki-operator -n openshift-operators-redhat --ignore-not-found
oc delete subscription cluster-observability-operator \
  -n openshift-cluster-observability-operator --ignore-not-found
oc delete subscription dynatrace-operator -n dynatrace-operator --ignore-not-found

# 6. Eliminar GitopsService y ArgoCD CR
oc delete gitopsservice cluster --ignore-not-found
oc delete argocd openshift-gitops -n openshift-gitops --ignore-not-found

# 7. Eliminar namespaces de demo
for ns in bookinfo istio-ingress istio-system istio-cni tracing-system openshift-logging grafana-system dynatrace-operator \
  openshift-gitops-operator openshift-tempo-operator openshift-operators-redhat \
  openshift-cluster-observability-operator openshift-gitops; do
  oc delete namespace "$ns" --ignore-not-found --wait=false
done
----

Si el namespace `openshift-gitops` queda en `Terminating`, elimina los finalizers del CR `ArgoCD`:

[source,bash]
----
oc patch argocd openshift-gitops -n openshift-gitops \
  -p '{"metadata":{"finalizers":[]}}' --type=merge
oc delete namespace openshift-gitops --ignore-not-found
----

== Problemas conocidos

* **Applications en `OutOfSync` con `forbidden`:** falta el `ClusterRoleBinding` del paso 4.
* **Pods de Bookinfo sin sidecar (`1/1`):** reinicia los deployments tras sincronizar el control plane.
* **MinIO bloqueado en sync:** la Application `minio` debe aplicarse antes que `observability`. El PVC y el Deployment comparten sync-wave `1` para evitar bloqueos con almacenamiento `WaitForFirstConsumer`.
* **Job `minio-create-buckets` fallido o Application `minio` en `Progressing`:** Argo CD lanza el Job en sync-wave `2` cuando el Deployment ya existe, pero MinIO puede no estar *Ready* aún. El chart incluye `readinessProbe` en MinIO y un bucle de espera con timeout en el Job. Si el Job falló en un intento anterior, bórralo y resincroniza:
+
[source,bash]
----
oc get jobs -n tracing-system
oc logs -n tracing-system job/minio-create-buckets
oc delete job minio-create-buckets -n tracing-system --ignore-not-found
oc delete job minio-create-bucket -n tracing-system --ignore-not-found  # nombre antiguo del chart
argocd app sync minio -n openshift-gitops
----
+
Si el error es `cannot patch` o `field is immutable` en el Job, es la inmutabilidad de Kubernetes: el chart usa `argocd.argoproj.io/hook-delete-policy: BeforeHookCreation` para recrear el Job en cada sync. Si persiste, elimina el Job manualmente como arriba.
+
El Job usa un *initContainer* (`curl`) que espera `GET /minio/health/ready` y solo entonces el contenedor `mc` ejecuta **una vez** `mc --config-dir /var/mc alias set` (sin bucle `until mc alias set`, que fallaba en OpenShift al no poder escribir la config de `mc` con el UID asignado por SCC). La config se guarda en un `emptyDir` montado en `/var/mc` con ServiceAccount + SCC `nonroot-v2`.
+
Si el pod del Job no arranca por SCC, comprueba: `oc describe pod -n tracing-system -l job-name=minio-create-buckets`.
+
Si no puede hacer pull de `quay.io/minio/mc` o `curlimages/curl`, sustituye `mcImage` / `bucketJob.waitImage` en `helm-charts/minio/values.yaml` por imágenes del mirror del cluster.
* **Grafana sin dashboard de logs o sin datasource Loki:** sincroniza la Application `logging` antes que `grafana`. Evita `ApplyOutOfSyncOnly=true` en la Application de Grafana (el Deployment debe montar el ConfigMap `grafana-dashboard-bookinfo-logs`).
* **Grafana sin logs de productpage/reviews:** genera tráfico (`curl -L …/productpage`); sincroniza apps `bookinfo-application`, `sm3-control-plane`, `logging` y `grafana`. En el dashboard usa **Container → All** o filtra `container="istio-proxy"` para reviews.
* **Operadores `Degraded` en Argo CD:** revisa el CSV en OLM; si está `Succeeded`, puedes ignorar el estado en Argo CD.
* **Tráfico HTTP en lugar de HTTPS:** el ingress usa terminación TLS en el edge; el script `generate-traffic.sh` usa `https://`.
